version: '3.7'
services:
  backend:
    build:
      context: .
      target: development
    ports:
      - ${PORT}:${PORT}
    volumes:
      - ./:/backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_URI=mongodb://mongodb:27017
  mongodb:
    image: mongo:latest
    volumes:
      - ./.data:/data/db
    ports:
      - 27018:27017
    restart: unless-stopped
